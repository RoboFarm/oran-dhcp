"""Forensics subpackage: pure-stdlib pcap + DHCP analysis.

Submodules: ``models``, ``pcap``, ``dhcp``, ``transactions``, ``detectors``,
``report``, ``cli``. No eager imports here so that partially-populated
checkouts still import.
"""
